if (!("a" in window)) {
var a = 1;
}
alert(a);

при использовании var все переменные создаются в самом начале в windows, 
а следовательно эта переменная уже будет лежать там и мы не зайдем в if.
А = undefined


var b = function a(x) {
x && a(--x);
};
alert(a);

переменная A не создана.


function a(x) {
return x * 2;
}
var a;
alert(a);

alert(a) выведет нам нашу функцию, поскольку она была создана раньше переменной.


function b(x, y, a) {
arguments[2] = 10;
alert(a);
}
b(1, 2, 3);

Судя по объяснениям с урока в A будет лежать 10.


```
function a() {
alert(this);
}
a.call(null);

в this будет положено null, который будет искаться вне функции
